name: hindsight-try
base: ubuntu@24.04
sdks:
  - name: try-hindsight
  - name: system
    plugs:
      hindsight-api:
        interface: tunnel
        endpoint: localhost:8888
      hindsight-webui:
        interface: tunnel
        endpoint: localhost:9999
