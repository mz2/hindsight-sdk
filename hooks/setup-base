apt-get update
eatmydata apt-get install curl ca-certificates

# Expose stable SDK entrypoints without putting the whole Python venv on PATH.
install -d /usr/local/bin
ln -sf "$SDK/bin/hindsight-api" /usr/local/bin/hindsight-api
ln -sf "$SDK/bin/hindsight-worker" /usr/local/bin/hindsight-worker
ln -sf "$SDK/bin/hindsight-admin" /usr/local/bin/hindsight-admin
ln -sf "$SDK/bin/hindsight-local-mcp" /usr/local/bin/hindsight-local-mcp
ln -sf "$SDK/bin/hindsight-control-plane" /usr/local/bin/hindsight-control-plane
